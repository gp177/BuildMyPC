@model BuildMyPC.ViewModels.IndexListViewModel
@{
    ViewBag.Title = "List";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Part List Builder</h2>
<hr />

<table class="table table-dark table-hover">
    <thread>
        <tr>
            <th>Component</th>
            <th>Selection</th>



        </tr>
    </thread>
    <tbody>


        <tr>
            <td>Cpu</td>
            <td>
                 @{ var cpu = Session["Cpu"]; }
                @if (cpu != null)
                {
                   <p>@cpu</p>
                }
                else
                {
                <a class="btn btn-primary btn-sm" href="~/CPU/Index">Pick a Cpu</a>
                }
            </td>
        </tr>
        <tr> 
            <td>Motherboard</td>
            <td>
                @{ var motherboard = Session["motherboard"]; }
                @if (motherboard != null)
    {
            <p>test</p>
}
else
{
            <a class="btn btn-primary btn-sm" href="~/MotherBoard/Index">Pick a Motherboard</a>
}
            </td>
        </tr>
        <tr>
            <td>Memory</td>
            <td>
                @{ var memory = Session["memory"]; }
                @if (memory != null)
    {
            <p>test</p>
}
else
{
            <a class="btn btn-primary btn-sm" href="~/Memory/Index">Pick Memory</a>
}
            </td>
        </tr>
        <tr>
            <td>Video Card</td>
            <td>
                @{ var videocard = Session["videocard"]; }
                @if (videocard != null)
    {
            <p>test</p>
}
else
{
            <a class="btn btn-primary btn-sm" href="~/VideoCard/Index">Pick a Video Card</a>
}
            </td>
        </tr>
        <tr>
            <td>Storage</td>
            <td>
                
                @{ var storage = Session["storage"]; }
                @if (storage != null)
    {
            <p>test</p>
}
else
{
            <a class="btn btn-primary btn-sm" href="~/Storage/Index">Pick Storage</a>
}
            </td>
        </tr>
        <tr>
            <td>Power Supply</td>
            <td>
                @{ var powersupply = Session["powersupply"]; }
                @if (powersupply != null)
    {
            <p>test</p>
}
else
{
            <a class="btn btn-primary btn-sm" href="~/PowerSupply/Index">Pick a PowerSupply</a>
}
            </td>
        </tr>
        <tr>
            <td>Case</td>
            <td>
                @{ var cases = Session["cases"]; }
                @if (cases != null)
    {
            <p>test</p>
}
else
{
            <a class="btn btn-primary btn-sm" href="~/Case/Index">Pick a Case</a>
}
            </td>
        </tr>
        <tr style="font-weight:bold">
            <td>Total Price</td>
            <td>$0.00</td>
        </tr>

    </tbody>
</table>


<a class="btn btn-primary" href="#">Share your Build!</a>
